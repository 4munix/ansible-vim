---
user: "{{ myuser if myuser is defined else 'root' }}"
home_dir: "{{ '/home/' ~ myuser if myuser is defined else '/root' }}"
vim_dir: "{{ home_dir }}/.vim"
vimrc: "{{ home_dir }}/.vimrc"
vim_colorscheme: "github"
